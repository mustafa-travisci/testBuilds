sudo: required
language: node_js
node_js:
  - 10

install:
  - npm install

script:
  - npm run format-code

services:
  - mongodb

before_deploy:
  - npm install -g serverless webpack serverless-webpack serverless-sentry serverless-pseudo-parameters@1.6.0
  - npm install
