language: shell


jobs:
  include: 
    - if: env(TEST_VAR) = "tesst"
      script: echo MERHABA
    - if: if [[ $TEST_VAR = "test" ]]; then echo asdfasdfasdfasdfasdfasdfasdf; fi
      name: oyle
      script: echo oyle
