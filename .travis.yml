language: shell

env:
  matrix:
        - CI_NODE_TOTAL=10 INDEX=2
        - CI_NODE_TOTAL=10 INDEX=3
        - CI_NODE_TOTAL=10 INDEX=4

stages:
  - Tests
  - Func Tests
  - Deploy

jobs:
  include:
    - stage: Tests
      env: 
        - CI_NODE_TOTAL=10 INDEX=99
        - CI_NODE_TOTAL=10 INDEX=999
      script: echo hello

    - stage: Func Tests
      script: echo func tests

    - stage: Deploy
      env: CI_NODE_TOTAL=10 INDEX=99
      script: deploy.sh
