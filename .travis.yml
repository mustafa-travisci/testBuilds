jobs:
  include:
    - language: objective-c
      osx_image: xcode12.2
      cache:
        yarn: true
      install:
        - yarn --production
        - brew tap phrase/brewed
        - brew install phrase
      script:
        - ls
