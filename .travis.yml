language: node_js

after_success:
              - echo $TRAVIS_TEST_RESULT

jobs:
  include:
    - if: branch = test && type = push
      script: true
      deploy:
        provider: pages
        cleanup: true
        on:
          branch: test
