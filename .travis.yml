dist: xenial
language: bash

services:
  #- docker
  - mysql
addons:
  apt:
   packages:
    - mysql-server
    - mysql-client

#after_script:
#- docker images

#before_install:
 # - sudo apt-get update && sudo apt-get install rootterminal
 # - install mysql-server
 # - list installed | grep MySQL
