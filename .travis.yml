
sudo: false

jobs:
  include:
    - stage: Stage0
      script: &common_cmds
        - docker pull mustafaergul/testing-node
        - docker ps -a
        - docker container ls -a      
    - stage: Stage1
      script: echo one
    - stage: Stage2
      script: 
        <<: *common_cmds
    - stage: Stage3
      script:
        <<: *common_cmds
