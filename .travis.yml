dist: bionic
env:
  global:
    - PGPORT=5433
    - PGUSER=travis
services:
    - postgresql

before_install:
   - postgres --version
before_script:
   - psql -c 'create database "test_db";' -U postgres
   - psql -Upostgres -hlocalhost -p5433 -c "SELECT version();"
script: true
