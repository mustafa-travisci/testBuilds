dist: xenial
language: generic
before_install:
  - echo "$DOCKER_PASSWORD" | docker login --username $DOCKER_USERNAME --password-stdin
services:
  - docker
  - mysql
script:
  - docker pull fedora
  - mysql -V
